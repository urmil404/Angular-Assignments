import { FruitsFilterPipe } from './fruits-filter.pipe';

describe('FruitsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FruitsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
