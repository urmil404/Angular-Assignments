import { FruitsDirective } from './fruits.directive';

describe('FruitsDirective', () => {
  it('should create an instance', () => {
    const directive = new FruitsDirective();
    expect(directive).toBeTruthy();
  });
});
