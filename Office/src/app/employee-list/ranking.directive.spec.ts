import { RankingDirective } from './ranking.directive';

describe('RankingDirective', () => {
  it('should create an instance', () => {
    const directive = new RankingDirective();
    expect(directive).toBeTruthy();
  });
});
